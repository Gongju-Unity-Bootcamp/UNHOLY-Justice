namespace Data
{
    /// <summary>
    /// 각 상태에 따른 stamina 소모량 혹은 획득량을 정의해놓은 클래스입니다.
    /// </summary>
    enum StaminaValues
    {
        jump = 10,
        sprint = 15,
        recovery = 15,
        dodge = 20,
        defenseHit = 20,
        parry = 50,
        onehand = 10,
        twohand = 20
    }
}
